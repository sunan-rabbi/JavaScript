const arr = [1, 2, 4, 5, 6, 7, 8, 9, 10]

// arr.push(11) // add element at last
// arr.pop() // remove last element

// arr.unshift(0) // add element at first
// arr.shift() // remove first element


//arr.sort() // 1, 10, 2, 4, 5, 6,  7, 8, 9 [Sort by string]
//
//arr.sort((a, b) => a - b) // 1, 2, 4, 5, 6, 7, 8, 9, 10 [Sort by number]


//arr.reverse() // 10, 9, 8, 7, 6, 5, 4, 2, 1 [Reverse the array]

//const newArr = arr.join("-") // 1-2-4-5-6-7-8-9-10


